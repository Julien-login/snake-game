name: Deploy Pages

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Upload Artifacts
        uses: actions/upload-artifact@v3
        with:
          name: github-pages
          path: ./  # Verwende das Root-Verzeichnis, falls deine HTML-Dateien dort sind

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
name: Deploy Pages

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Build Artifacts
        run: |
          # Hier kannst du den Befehl zum Erstellen der Artefakte hinzufügen
          echo "Build artifacts"
      
      - name: Upload Artifacts
        uses: actions/upload-artifact@v3
        with:
          name: github-pages
          path: ./path-to-your-build-output  # Passe den Pfad zu deinem Build-Output an

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
